# Secrets

One password per file, no trailing newline. Generated by `make secrets`.
`*.txt` is gitignored — never commit real credentials.

- `mysql_root_password.txt` — MySQL root
- `mysql_app_password.txt`  — MySQL app user (`MYSQL_USER`)
- `redis_password.txt`      — Redis `requirepass`
